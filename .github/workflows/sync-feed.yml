name: Sync ADIMA feed z FTP (Ploberger)

on:
  schedule:
    - cron: '20 4,7,10 * * *'   # 6:20, 9:20, 12:20 SELČ
  workflow_dispatch: {}  # umožní ruční spuštění tlačítkem v záložce Actions

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repozitáře
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Stažení XML z FTP
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_PORT: ${{ secrets.FTP_PORT }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASS }}
        run: |
          curl -sS --fail \
            "ftp://${FTP_HOST}:${FTP_PORT}/adima_catexp_3968.xml" \
            --user "${FTP_USER}:${FTP_PASS}" \
            --connect-timeout 30 --retry 3 --retry-delay 10 \
            -o adima_catexp_3968.xml

      - name: Kontrola, že soubor není prázdný nebo poškozený
        run: |
          test -s adima_catexp_3968.xml
          python3 -c "import xml.etree.ElementTree as ET; ET.parse('adima_catexp_3968.xml')"

      - name: Commit a force-push (jeden commit, aby repozitář neuhromě rostl)
        run: |
          git config user.name "feed-bot"
          git config user.email "feed-bot@users.noreply.github.com"
          git add adima_catexp_3968.xml
          git commit --amend -m "Aktualizace feedu $(date -u +'%Y-%m-%d %H:%M UTC')" --allow-empty
          git push --force origin HEAD:main
